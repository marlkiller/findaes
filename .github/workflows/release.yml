name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux
            ext:
            binary_name: findaes-linux
          - os: macos-latest
            target: macos
            ext:
            binary_name: findaes-macos
          - os: windows-latest
            target: windows
            ext: .exe
            binary_name: findaes-windows

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build on Linux/macOS
        if: matrix.target != 'windows'
        run: |
          make clean
          make

      - name: Build on Windows
        if: matrix.target == 'windows'
        run: |
          choco install mingw -y
          make clean
          make cross

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary_name }}
          path: findaes${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./release
          pattern: findaes-*
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./release/findaes-linux/findaes
            ./release/findaes-macos/findaes
            ./release/findaes-windows/findaes.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
